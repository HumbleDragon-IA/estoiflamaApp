// app/_components/Footer.jsx
function Footer() {
  return (
    <footer className="border-t pb-[env(safe-area-inset-bottom)]  bg-stone-900">
      <div className="container mx-auto px-4 sm:px-6 lg:px-8 py-4 text-center  text-sm text-stone-50">
        EstoiFlama — “No son las cosas las que nos perturban, sino la opinión
        que tenemos sobre ellas.”
      </div>
    </footer>
  );
}

export default Footer;
